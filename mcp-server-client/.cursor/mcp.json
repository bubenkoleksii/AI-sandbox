{
  "mcpServers": {
    "sandbox-server": {
      "url": "http://localhost:3000/mcp",
      "auth": {
        "CLIENT_ID": "${env:AUTH0_CLIENT_ID}",
        "scopes": [
          "openid",
          "profile",
          "mcp:read",
          "mcp:write"
        ]
      }
    }
  }
}
